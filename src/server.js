// const app = require('./app');
// const { connectDB } = require('./config/db');

// const PORT = process.env.PORT || 5000;

// connectDB();


// app.get('/', (req, res) => {
//     res.send('UrbanDrive is running...');
// })
// app.listen(PORT, () => {
//   console.log(`Server is running on port ${PORT}`);
// });
